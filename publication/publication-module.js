'use strict';
/*
 * Ideally we would want to change the full record from a mere controller to a directive
 * this way, we can implement the full record details in any fashion we want
 */
(function (angular) {

  angular
    .module('ne.views.PublicationModule', ['ui.router']);

})(angular);
